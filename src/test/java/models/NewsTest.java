package models;
import models.*;
import org.junit.*;
import static org.junit.Assert.*;

import static org.junit.Assert.*;

public class NewsTest {
    @Rule
    public DatabaseRule database = new DatabaseRule();
}
